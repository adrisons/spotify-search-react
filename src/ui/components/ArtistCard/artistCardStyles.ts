export const artistCardSizeClass = "h-[220px] w-[168px] shrink-0";

/** Padding around each card so hover scale/shadow is not clipped by the carousel. */
export const artistCarouselItemClass = "shrink-0 snap-start p-3";
